spring:
  application:
    name: config-server  # 应用名称
  cloud:
    config:
      server:
        git:
          uri: https://github.com/okgogo27/ConfigDemo #配置文件所在仓库
          username: 1024030474@qq.com
          password: sysuwangxd27
          default-label: master #配置文件分支
          search-paths: config  #配置文件所在根目录

eureka:
  client:
    service-url:
      defaultZone: http://localhost:5000/eureka/